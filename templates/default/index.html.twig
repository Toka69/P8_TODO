{% extends 'base.html.twig' %}

{% block header_title %}
    <h1>Bienvenue sur Todo List, l'application vous permettant de gérer l'ensemble de vos tâches sans effort !</h1>
{% endblock %}

{% block body %}

    <div class="d-grid gap-2 mx-auto d-md-block">
        <a href="{{ path('task_create') }}" class="btn btn-success me-md-1">Créer une nouvelle tâche</a>
        <a href="{{ path('task_list') }}" class="btn btn-info me-md-1">Consulter la liste des tâches à faire</a>
        <a href="{{ path('task_done') }}" class="btn btn-secondary">Consulter la liste des tâches terminées</a>
    </div>

{% endblock %}